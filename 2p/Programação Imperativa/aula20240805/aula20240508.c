#include <stdio.h>

int main()
{
    printf("hello pucpr\n");

    //######### NUMEROS (INT, SHORT, LONG, FLOAT, DOUBLE, LONG DOUBLE)

    // double pi = 3.14;
    // double raio = 10;

    // double area = pi * raio * raio;
    
    // printf("área do circulo: %.2f\n", area);

    // printf("%zu\n", sizeof(int));
    // printf("%zu\n", sizeof(short));
    // printf("%zu\n", sizeof(long));

    // printf("%p\n", &area);
    // printf("%p\n", &raio);
    // printf("%p\n", &pi);

    //######## CHAR (LETRA UNICA)

    // char letra = 'A';
    // printf("%c\n", letra);
    // printf("%d\n", letra);

    // letra = letra + 1;
    // printf("%c\n", letra);
    // printf("%d\n", letra);

    // letra = letra / 2;
    // printf("%c\n", letra);
    // printf("%d\n", letra);

    // printf("%zu\n", sizeof(char));

    // ############## EX 3.1

    // int temperatura = 20;
    // temperatura = -10;

    // unsigned int idade = 30;
    // idade = 40;

    // printf("%p\n", &idade);
    // printf("%p\n", &temperatura);
    // printf("%zu\n", sizeof(idade));
    // // 78 - 74 (temperatura)
    // // 74 - 70 (idade)

    return 0;
}